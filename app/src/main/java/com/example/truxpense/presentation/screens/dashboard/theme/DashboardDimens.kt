package com.example.truxpense.presentation.screens.dashboard.theme

import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

// Dimension tokens used across dashboard screens (spacing, sizes, fonts)
object DashboardDimens {

    // Spacing scale
    /** 2 dp — micro gap */
    val spaceXxs = 2.dp

    /** 4 dp — tight gap */
    val spaceXs = 4.dp

    /** 6 dp — small gap */
    val spaceSm = 6.dp

    /** 8 dp — standard gap */
    val spaceMd = 8.dp

    /** 10 dp — card sub-section gap */
    val spaceMdL = 10.dp

    /** 12 dp — card internal padding */
    val spaceLg = 12.dp

    /** 16 dp — outer horizontal padding */
    val spaceXl = 16.dp

    /** 24 dp — section separator */
    val spaceXxl = 24.dp

    /** 32 dp — large section gap */
    val spaceXxxl = 32.dp

    // Component heights
    /** Standard tappable button height */
    val buttonHeight = 48.dp

    /** Small icon button */
    val iconButtonSm = 28.dp

    /** Bottom navigation bar row height */
    val bottomNavHeight = 68.dp

    /** Progress bar track height */
    val progressBarHeight = 7.dp

    /** Chart divider */
    val dividerHeight = 1.dp

    // Icon sizes
    /** Micro icon */
    val iconXs = 14.dp

    /** Small icon */
    val iconSm = 16.dp

    /** Medium icon */
    val iconMd = 20.dp

    /** Standard icon */
    val iconLg = 24.dp

    /** Category color dot */
    val dotSize = 10.dp

    // Chart dimensions
    /** Donut chart canvas size */
    val donutChartSize = 200.dp

    /** Donut ring stroke width */
    val donutStrokeWidth = 38.dp

    /** Spending trend chart height */
    val trendChartHeight = 120.dp

    /** Budget detail chart height */
    val budgetChartHeight = 110.dp

    // Corner radii
    /** Section card corners */
    val cornerCard = 14.dp

    /** Chip corners */
    val cornerChip = 8.dp

    /** Badge corners */
    val cornerBadge = 4.dp

    /** Period toggle outer */
    val cornerToggleOuter = 8.dp

    /** Period toggle inner */
    val cornerToggleInner = 6.dp

    // Card / container padding
    /** Standard internal card padding */
    val cardPadding = spaceXl

    /** Compact card padding */
    val cardPaddingComp = spaceLg

    /** Dropdown list horizontal padding */
    val listPaddingH = spaceMd

    /** Dropdown list vertical padding */
    val listPaddingV = spaceLg

    /** Screen horizontal edge padding */
    val screenPaddingH = spaceXl

    // Font sizes
    /** Helper / caption */
    val textXs = 9.sp

    /** Fine print */
    val textSm = 10.sp

    /** Label / secondary */
    val textMd = 12.sp

    /** Body copy */
    val textLg = 13.sp

    /** Button label */
    val textXl = 15.sp

    /** Section title */
    val textXxl = 16.sp

    /** Helper line-height */
    val lineHeightHelper = 18.sp

    // Tokens for AddExpense / HomeTab / BudgetTab
    /** Large hero amount text */
    val textHero = 40.sp

    /** Section screen padding top */
    val heroZonePadTop = 24.dp
    val heroZonePadBottom = 28.dp

    /** FAB clearance at bottom */
    val fabClearance = 72.dp

    /** Thin row divider */
    val dividerThin = 0.5.dp

    /** Small button height */
    val buttonHeightSm = 32.dp

    /** Consistent progress bar height */
    val progressBarHeight2 = 6.dp

    /** Notes card horizontal padding */
    val detailCardPadH = spaceLg
    val detailCardPadV = 14.dp

    /** Row vertical padding */
    val rowPadV = spaceXl
}